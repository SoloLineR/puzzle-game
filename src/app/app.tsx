import { Button } from "@/components/ui/button";

export const App = () => {
  return (
    <div>
      <Button variant={"outline"} className="cursor-pointer">
        click me
      </Button>
    </div>
  );
};
